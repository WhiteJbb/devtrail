# CLAUDE.md

**이 레포의 개발 규칙 정본은 [`AGENTS.md`](./AGENTS.md)다. 작업 시작 전 먼저 읽는다.**

아키텍처 지도, 코드 배치·스타일·테스트, 브랜치·커밋·PR, 세션 생명주기(MCP /
capture-session), Vault 구조와 권한, 후보 흐름 — 전부 `AGENTS.md`에 있다.

이 파일은 규칙을 중복해 적지 않는다. 규칙이 바뀌면 `AGENTS.md`만 고친다.
