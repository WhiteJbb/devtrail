# TODO — 2026-06-27

지금까지 구축된 자동화 인프라 기반으로, 신뢰성·UX·운영 가시성 관점 개선 항목.

---

## 1. 테스트 정비 (신뢰성 기반)

현재 14개 테스트 실패 중. 코드 변경 시 회귀를 감지할 수 없는 상태.

- [ ] `python -m pytest tests/ -q` 전체 실행해서 실패 목록 파악
- [ ] 기존 코드 변경으로 깨진 것 vs 테스트 자체가 잘못된 것 분류
- [ ] 실패 테스트 수정 또는 삭제
- [ ] CI가 없다면 pre-push hook으로 `pytest -q` 추가 고려

---

## 2. 데이터 마이그레이션 (vault 경로 정합성)

코드는 새 경로를 보는데 기존 파일이 구 경로에 그대로 있어 distill 대상에서 빠짐.

- [ ] `00_Inbox/Captures/` 18개 파일 → `URLs/` 또는 `Memos/`로 분류 이동
- [ ] `10_Worklog/Daily/` 세션 파일 4개 → `10_Worklog/Sessions/`로 이동
- [ ] 이동 후 `wiki_service.scan_notes()`가 제대로 잡는지 확인

---

## 3. nightly 파이프라인 점검

자동화 핵심 경로인데 실제로 잘 돌고 있는지 확인이 안 된 부분이 있음.

- [ ] `open_loops_agent`가 nightly 파이프라인에 실제로 연결됐는지 확인
  - `60_Candidates/MemoryPatches/`가 비어있어서 의심
- [ ] nightly 실행 결과(성공/실패)를 텔레그램으로 요약 알림
  - 현재는 오류만 알림. "오늘 후보 N개 생성" 수준이라도 있으면 파이프라인 상태 파악 가능
- [ ] nightly 실패 시 다음날 중복 실행 방지 로직 확인

---

## 4. 서버 코드 자동 업데이트

지금은 서버에서 SSH + `git pull` + `/restart`를 수동으로 해야 반영됨.

- [ ] `/deploy` 텔레그램 명령 추가: `git pull` → 봇 재시작
  - `work-agent-update` 태스크가 이미 있다면 그걸 트리거하는 방식으로
- [ ] 또는 `work-agent-update` 태스크가 실제로 자동 pull을 하는지 확인

---

## 5. Stale 후보 자동 정리

`is_stale`(14일 초과) 플래그가 있지만 아무 동작도 안 함. 후보가 무기한 누적됨.

- [ ] 30일 초과 후보 자동 삭제 or 텔레그램 알림 ("오래된 후보 N개 있음")
- [ ] `/review` 시작 시 stale 후보 수 표시 ("⚠️ stale 3개 포함")
- [ ] 당장 `60_Candidates/CareerBullets/` 24개 검토 (/review로)

---

## 6. 봇 UX 소개선

- [ ] `/review` 버튼 4개가 모바일에서 좁을 수 있음 → 2행(승격/건너뛰기 | 삭제/종료)으로 분리 검토
- [ ] `/restart` 후 진행 중이던 review 큐가 날아가는 문제
  - 최소한 재시작 전 "검토 중인 후보가 있음" 경고라도
- [ ] 도움말(`/help`)에서 `/restart`, `/review` 삭제 버튼 언급 업데이트

---

## 우선순위 제안

| 순서 | 항목 | 이유 |
|------|------|------|
| 1 | **데이터 마이그레이션** (2번) | 지금 distill이 파일을 못 찾고 있을 가능성. 5분짜리 |
| 2 | **nightly 파이프라인 점검** (3번) | 핵심 자동화인데 실제로 돌고 있는지 모름 |
| 3 | **테스트 정비** (1번) | 이후 코드 변경의 안전망 |
| 4 | **서버 자동 업데이트** (4번) | 현재 매번 수동이 불편함 |
| 5 | **Stale 후보 정리** (5번) | CareerBullets 24개부터 /review로 처리 |
